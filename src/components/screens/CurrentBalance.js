import React, { Component } from 'react';

import Layout from '../layout/Base';

class CurrentBalance extends Component {
  render () {
    return (
      <Layout>
        <h1>Current Balance page</h1>
      </Layout>
    );
  }
}

export default CurrentBalance;